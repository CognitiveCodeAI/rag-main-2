"""Access Control Layer (ACL) for NPR.

Provides tenant-scoped, ABAC/RBAC document-level permissions
with defense-in-depth enforcement at every pipeline stage.

Feature-flagged via ACL_ENABLED (default: false).
"""
