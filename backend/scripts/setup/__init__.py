"""Setup scripts for NPR RAG application.

Usage:
    python -m scripts.setup.setup_all      # Run all setup steps
    python -m scripts.setup.setup_postgres  # PostgreSQL only
    python -m scripts.setup.setup_milvus    # Milvus only
    python -m scripts.setup.setup_minio     # MinIO only
    python -m scripts.setup.verify_connections  # Verify all connections
"""
