# Build/runtime shim: pymilvus 2.4.9 imports pkg_resources, which setuptools>=81 removed.
# Pin below 81 so a fresh venv (esp. Python 3.12+, which omits setuptools by default) keeps pkg_resources.
setuptools<81

# FastAPI and server
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.18

# Data validation
pydantic==2.10.4
pydantic-settings==2.7.1
jsonschema>=4.20.0

# CORS and HTTP
httpx==0.28.1

# Environment and config
python-dotenv==1.0.1
# pymilvus->environs currently requires marshmallow v3 APIs
marshmallow<4

# Streaming
sse-starlette==2.2.1

# Date/time
python-dateutil==2.9.0

# Database - PostgreSQL + SQLAlchemy + Alembic
psycopg2-binary==2.9.10
sqlalchemy==2.0.36
alembic==1.14.0

# Vector database
pymilvus==2.4.9

# OpenSearch
opensearch-py==2.8.0

# MinIO (S3)
minio==7.2.15

# Neo4j
neo4j==5.28.1

# Task queue
celery==5.4.0
redis==5.2.1

# Document parsing
PyMuPDF==1.27.2.3
pdfplumber==0.11.4
python-docx==1.1.2
beautifulsoup4==4.12.3
lxml==5.3.0
pandas==2.2.3
openpyxl==3.1.5
markdown==3.7

# Tokenization (for chunking)
tiktoken==0.8.0

# Embeddings
openai==2.15.0

# Docling (optional - multi-format document ingestion)
# Install separately: pip install docling>=2.72.0
# docling>=2.72.0
